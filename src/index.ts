const name = "Nicolas",
  age = 24,
  gender = "male";

const sayHi = (name, age, gender?) => {
  console.log(`Hello ${name}! ${age}, anqlwkjdd you are also ${gender}`);
};

sayHi(name, age, gender);

export {};
